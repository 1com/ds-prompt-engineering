[project]
name = "ds-prompt-engineering"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "langchain-groq>=1.1.3",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
